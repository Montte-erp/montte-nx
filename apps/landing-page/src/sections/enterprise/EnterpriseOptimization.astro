---
import { Headphones, Shield, Zap } from "@lucide/astro";
import { Separator } from "@packages/ui/components/separator";

interface OptimizationFeature {
   icon: string;
   title: string;
   description: string;
}

interface Props {
   features?: OptimizationFeature[];
}

const defaultFeatures: OptimizationFeature[] = [
   {
      icon: "Zap",
      title: "API access",
      description:
         "Integre o Montte com suas ferramentas. Automatize importações, exporte dados e construa fluxos personalizados.",
   },
   {
      icon: "Shield",
      title: "Modelos de juros",
      description:
         "Calcule juros compostos, simples ou personalizados. Projeções financeiras precisas para seu negócio.",
   },
   {
      icon: "Headphones",
      title: "Dashboards avançados",
      description:
         "Dashboards personalizados, fluxo de caixa, análises por centro de custo. Exporte em PDF ou compartilhe com sua equipe.",
   },
];

const { features = defaultFeatures } = Astro.props;
---

<section  class="relative px-8 py-18 md:py-28 flex flex-col gap-12 items-center container max-w-7xl mx-auto overflow-hidden">
     <div class="flex flex-col gap-12 items-center z-10">

    <p class=" text-sm text-primary  uppercase font-bold text-center tracking-widest">
      Todas as funcionalidades
         </p>

   <h3 class="
 inline-block font-semibold tracking-tight text-center text-3xl md:text-5xl leading-tight max-w-3xl text-foreground
      ">
               Tudo incluso no plano ERP

         </h3>

   </div>



  <div class=" flex flex-col md:flex-row items-center md:items-start gap-10">
    {features.map((feature) => (
      <div class="flex-1  flex flex-col gap-6">
      <div class="flex flex-col items-start gap-4">
          <div class="text-primary">
            {feature.icon === "Zap" && <Zap class="w-6 h-6" />}
            {feature.icon === "Shield" && <Shield class="w-6 h-6" />}
            {feature.icon === "Headphones" && <Headphones class="w-6 h-6" />}
        </div>
        <Separator className="bg-primary" />
        </div>
        <p class=" text-foreground font-semibold lg:text-lg">{feature.title}</p>
        <p class=" text-muted-foreground">{feature.description}</p>
      </div>
    ))}
  </div>
</section>

