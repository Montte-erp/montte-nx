---
import { MessageSquare, Users } from "@lucide/astro";
import {
   Card,
   CardContent,
   CardDescription,
   CardTitle,
} from "@packages/ui/components/card";

const features = [
   {
      icon: "MessageSquare",
      title: "Suporte prioritário por email",
      description:
         "Respostas rápidas de quem entende do sistema. Dúvidas sobre automações, importação ou dashboards resolvidas sem demora.",
   },
   {
      icon: "Users",
      title: "7 dias de teste grátis",
      description:
         "Experimente todas as funcionalidades do plano ERP sem compromisso. Sem cartão de crédito para começar.",
   },
] as const;

const iconComponents = {
   MessageSquare,
   Users,
};
---

<section  class="relative px-8 py-18 md:py-28 flex flex-col gap-12 items-center container max-w-7xl mx-auto overflow-hidden">
  <div class="flex flex-col gap-6 items-center z-10">
    <p class="text-sm uppercase font-bold text-center tracking-widest text-primary">
      Suporte prioritário
    </p>

    <h2 class="inline-block font-semibold tracking-tight text-center text-3xl md:text-5xl leading-tight max-w-3xl text-foreground">
      Chamada 1:1 para configurar suas automações
    </h2>

    <p class="sm:text-xl text-center max-w-sm sm:max-w-xl text-foreground/70">
      No plano ERP, você tem uma chamada de setup incluída.
      Configuramos suas automações, centros de custo e dashboards junto com você.
    </p>
  </div>

  <div class="w-full grid grid-cols-1 lg:grid-cols-2 gap-6 flex-1">
    {features.map((feature) => {
      const IconComponent = iconComponents[feature.icon];
      return (
        <Card className="flex-1 bg-gradient-to-br from-primary/5 via-primary/10 to-primary/5 border-primary/20">
          <CardContent className="flex flex-col gap-4 items-start">
            <div class="size-12 rounded-lg bg-primary/10 flex items-center justify-center">
              <IconComponent 
                class="size-6 stroke-primary stroke-[1.5]" 
                aria-hidden="true" 
              />
            </div>
            <CardTitle className="font-semibold text-xl text-foreground">
              {feature.title}
            </CardTitle>
            <CardDescription className="text-base text-muted-foreground leading-relaxed">
              {feature.description}
            </CardDescription>
          </CardContent>
        </Card>
      );
    })}
  </div>
</section>
