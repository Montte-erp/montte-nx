import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerTools(_server: McpServer) {
   // No MCP tools registered — CMS tools were removed during ERP pivot
}
