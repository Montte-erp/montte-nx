interface:
  display_name: "Better Result"
  short_description: "Typed Result flows for Minutei"
  default_prompt: "Use $better-result to implement typed domain errors, Result.gen composition, oRPC boundary mapping, and focused tests in Minutei AI."

policy:
  allow_implicit_invocation: true
