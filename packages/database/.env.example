DATABASE_URL=postgresql://postgres:password@localhost:5433/finance_tracker

# Server-side encryption (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=

# Search key for blind index encryption (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SEARCH_KEY=
